# PrithviQ frontend environment (copy to .env.local for local development).
VITE_API_BASE_URL=http://localhost:8000/api/v1

# When "true", the app uses the built-in demo data (src/data/mock.ts) instead
# of the backend API. Leave unset or "false" to call the real backend.
VITE_DEMO_MODE=false
